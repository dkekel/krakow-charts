apiVersion: v1
kind: Service
metadata:
  name: krakow-service
spec:
  selector:
    app.kubernetes.io/name: {{ .Values.krakow.service.selector }}
  ports:
    - protocol: TCP
      port: {{ .Values.krakow.server.port.external }}
      targetPort: {{ .Values.krakow.server.port.internal }}
